#include<iostream>

int main() 
{
    int t;
    std::cin>>t;
    while(t--) 
    {
        long long x,y,z, modulo;
        std::cin>>x;
        y += x + 1;
        z += z + 1;
        while(z != 0 )
        {
            modulo = z % 10;

        }

    }
    return 0;
}
